$.round_title.text = $.args.round_title || '';
$.round_subtitle.text = $.args.round_subtitle || '';
$.round_counter.text = $.args.round_counter || '';

var source = [
{
	foo:'uno',
	bar:'dos',
},
{
	foo:'tres',
	bar:'cuatro'
}
];

// var arg = {
// 	title: source[0].foo,
// 	value: source[0].bar,
// };
// var btn = Alloy.createController('button', arg).getView();


// var source = elements;
// for (var i=0; i<source.length; i++) {    
//     var arg = {
//         title: source[i].postTitle,
//         value: source[i].postLink
//     };
//   var btn = Alloy.createController('button', arg).getView();

// }
// $.round_intro.add()